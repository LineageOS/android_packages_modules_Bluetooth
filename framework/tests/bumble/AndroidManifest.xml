<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.bluetooth" >

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_BLUETOOTH_SHARE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.LOCAL_MAC_ADDRESS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />

    <application>
        <uses-library android:name="android.test.runner" />

        <receiver android:name="android.bluetooth.PendingIntentScanReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.test.ACTION_SCAN_RESULT" />
            </intent-filter>
        </receiver>

        <provider
            android:authorities="android.bluetooth.opp"
            android:name=".opp.AllowedTestImageProvider"
            android:exported="true"
            android:readPermission="android.permission.BLUETOOTH"
            android:grantUriPermissions="true">
        </provider>

        <provider
            android:authorities="android.bluetooth.opp.restricted"
            android:name=".opp.RestrictedTestImageProvider"
            android:exported="true"
            android:readPermission="android.permission.ACCESS_BLUETOOTH_SHARE"
            android:grantUriPermissions="true">
        </provider>
    </application>

    <instrumentation android:name="androidx.test.runner.AndroidJUnitRunner"
                     android:targetPackage="android.bluetooth"
                     android:label="Bumble Bluetooth Test"/>
</manifest>
